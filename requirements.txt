flask

Flask==3.1.2
Werkzeug==3.1.3
Jinja2==3.1.6
itsdangerous==2.2.0
blinker==1.9.0

# Agregar a requirements.txt
flask-socketio==5.3.5
python-socketio==5.10.0

# En app.py
from flask_socketio import SocketIO, emit

socketio = SocketIO(app, cors_allowed_origins="*")

@socketio.on('connect')
def handle_connect():
    print(f'Cliente conectado: {request.sid}')

# Emitir cuando hay nueva contraoferta
def notificar_nueva_oferta(pasajero_id, contraoferta):
    socketio.emit('nueva_oferta', contraoferta, 
                  room=f'pasajero_{pasajero_id}')